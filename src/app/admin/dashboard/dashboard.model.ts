export class Counts {
    success!: boolean;
    counts!: Counts;

    data!: {
      users: number;
      orders: number;
      categories: number;
      products: number;
      // counts: Counts;
    };
  }
  