export class Counts {
    success!: boolean;
    counts!: Counts;

    data!: {
      users: number;
      companies: number;
      categories: number;
      products: number;
      // counts: Counts;
    };
  }
  